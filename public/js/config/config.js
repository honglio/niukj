define(function() {
    "use strict";
    var config = {
        slide: {
            size: {
                width: 1024,
                height: 666
            }
        }
    };

    return config;
});
